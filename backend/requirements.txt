# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
pgvector>=0.2.3

# Auth
python-jose[cryptography]>=3.3.0
# Pinned: passlib 1.7.4 + bcrypt 4.0.1 are the only compatible pair.
# bcrypt >=4.1 removed __about__ which passlib uses internally → AttributeError
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# ML — CPU-only torch to keep image small
torch>=2.1.0
sentence-transformers>=2.2.2
scikit-learn>=1.3.0
numpy>=1.24.0
scipy>=1.11.0

# CV parsing
pdfplumber>=0.10.0
python-docx>=1.0.0

# Scheduler
apscheduler>=3.10.0

# Settings
pydantic-settings>=2.0.0
pydantic[email]>=2.0.0

# Data
pandas>=2.0.0
